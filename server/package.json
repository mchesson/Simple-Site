{
  "name": "ts-workspace-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend for TS Workspace: Postgres data plane, Claude API assistant, and a glass-box inspector.",
  "scripts": {
    "db:reset": "node src/seed.js --reset",
    "seed": "node src/seed.js",
    "start": "node src/server.js",
    "test": "node test/setup.mjs && node --test --test-concurrency=1 test/*.test.mjs",
    "db:bootstrap": "echo 'run as a superuser: psql -d ts_workspace -f src/bootstrap.sql'",
    "setup": "./setup.sh"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=22"
  }
}
